The Less package contains a text file viewer.

1. Extraction
		
	tar -xvf less-679.tar.gz
	cd less-679

2. Building
	
	time { ./configure --prefix=/usr --sysconfdir=/etc && make && maek check && make install; }

