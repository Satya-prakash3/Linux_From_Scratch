Here we will build the Cross compiler, Some temporary tools and enter the chroot environment

The main goal here is to create a clean temporary area containing ome set of tools for good, and isolated from host system.

This build process is based on cross-compilation. Cross-compilation is normally used to build a compiler and its associated toolchain for a machine different from the one that is used for the build. 

